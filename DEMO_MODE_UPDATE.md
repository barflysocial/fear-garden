# Demo Mode Update

Players can enter the access code `BEERGARDEN` on the player page to launch Demo Mode. Demo Mode creates or reuses a private `DEMO` session, starts the game automatically, and bypasses the host start requirement. It still requires Terms & Conditions acknowledgment and first/last name entry.
