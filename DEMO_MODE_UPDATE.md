# Demo Mode Update

Demo mode is still supported internally, but the player join screen no longer displays demo instructions. The access code field now allows longer codes so internal demo access can be typed without being cut off.
